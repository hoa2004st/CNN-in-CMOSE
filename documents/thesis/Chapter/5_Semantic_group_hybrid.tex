\documentclass[../main.tex]{subfiles}
\begin{document}

\FloatBarrier
\section{Overview}
\label{sec:hybrid-overview}

Chapter~4 fixed the evaluation protocol --- QWK, macro-accuracy, and macro-MAE as the primary metrics; CMOSE as the development corpus; cross-entropy as the development loss; the OpenFace TCN at QWK 0.537 as the bar --- and supplied the design motivation: the baseline configurations err on different clips, leaving headroom that naive voting cannot claim, and the OpenFace and I3D feature families in particular fail on different clips.
This chapter evaluates the proposed \textbf{semantic-group hybrid} (specified in Section~\ref{sec:hybrid}) against that frame: all $3^{5}=243$ per-group encoder configurations, each with and without the I3D stream, trained with cross-entropy.
Section~\ref{sec:ablation} first re-validates the Chapter~4 frame on this 486-configuration population --- if the protocol were an artefact of five hand-picked baselines, that is where it would break (RQ1 in part).
Section~\ref{sec:group-marginal} isolates which behavioural group's encoder choice matters, in-domain \emph{and} under shift (RQ2).
Section~\ref{sec:i3d-effect} measures the effect of I3D fusion with a paired ablation across evaluation regimes (RQ3).
Section~\ref{sec:error} pins down the headline in-domain comparison with the baselines and localises the gain class by class (RQ1).
Section~\ref{sec:private} then takes the hybrid out of domain: it confirms that the cross-dataset collapse is architecture-independent, and establishes the new result that the hybrid generalizes better than the baselines --- in \emph{every} cell of the matrix, and most of all on the decisive self-collected private set, where the architecture and the corpus-pooling lever \emph{compound} (RQ4).
Section~\ref{sec:gen-discussion} discusses why, and reads the headline number honestly.
As in Chapter~4, accuracy appears only as a foil; every conclusion is argued from the primary metrics, with family-level claims reported as means and distributions and headline comparisons as the tuned maximum (the aggregation policy of Section~\ref{sec:baseline-overview}).

\FloatBarrier
\section{Consistency with the Baseline Frame}
\label{sec:ablation}

The ablation population doubles as a stress test of Chapter~4's protocol: 486 configurations of a different architecture family, none of which existed when the frame was fixed.
Figure~\ref{fig:hybrid_ablation_all_metrics} plots the distribution of the two hybrid families (with and without I3D) on every metric, each panel against the best base model on that metric.
Three findings of Chapter~4 reproduce.

\begin{figure}[ht]
\centering
\includegraphics[width=0.98\linewidth]{hybrid_ablation_all_metrics.png}
\caption{Hybrid ablation across all 243 per-group encoder configurations, with and
without I3D, on all six metrics (in-domain CMOSE). Each dashed line is the best base
model on that metric, across all three losses.}
\label{fig:hybrid_ablation_all_metrics}
\end{figure}

\textbf{The metric structure reproduces.}
Recomputing the Kendall rank correlation between the six metrics over the 486 in-domain configurations (Figure~\ref{fig:metric_correlation_hybrid}) yields the same two-block picture as Section~\ref{sec:protocol-metrics}: a micro block ($\tau=0.87$), a macro block ($\tau=0.73$), weak cross-block agreement ($\tau=0.26$--$0.33$), and QWK again the only bridge ($\tau=0.63$--$0.73$ with both blocks).
The block structure is a property of the metrics on this task, not of any model family.

\begin{figure}[ht]
\centering
\includegraphics[width=0.78\linewidth]{metric_correlation_hybrid.png}
\caption{Rank agreement (Kendall $\tau$) between the six metrics over the 486 hybrid
configurations, in-domain on CMOSE: the same two-block structure as for the baselines
(Figure~\ref{fig:metric_correlation_base}), with QWK again the only bridging metric.}
\label{fig:metric_correlation_hybrid}
\end{figure}

\textbf{Accuracy is still blind.}
In Figure~\ref{fig:hybrid_ablation_all_metrics} every configuration sits in a narrow accuracy band around the best baseline's 0.773 (top-left panel), so a model chosen on accuracy would see no architecture signal at all, while the QWK panel separates the families cleanly --- the same blindness Section~\ref{sec:protocol-metrics} argued for the baselines, now at the scale of 486 configurations.
The macro panels carry one honest caveat: their dashed lines (macro-accuracy 0.635, macro-MAE 0.458) belong to the \emph{ordinal-loss} I3D MLP, a balanced-loss operating point that buys those figures by giving up QWK (0.487, below the bar) --- the loss trade-off of Section~\ref{sec:loss-tradeoff}, not an architecture effect; no CE-trained model, baseline or hybrid, reaches it.

\textbf{The cross-domain collapse reproduces.}
Taken out of domain (Section~\ref{sec:hybrid-crossdomain}), the hybrid matrix shows the same shape as the baseline matrix: a healthy diagonal (0.605 on CMOSE, 0.228 on DAiSEE) and an off-diagonal collapse, with accuracy again masking it.
The collapse is therefore a property of the \emph{problem} --- divergent label priors and recording conditions --- not of a particular architecture.

\textbf{Conclusion.}
All three pillars of the Chapter~4 frame survive a 32-fold larger model population.
The protocol carries over without amendment, and the findings it produced can be treated as task properties (RQ1's frame condition); the remaining sections use it to answer the architecture questions.

\FloatBarrier
\section{Encoder Choice per Group}
\label{sec:group-marginal}

The 243-configuration grid varies five group$\rightarrow$encoder choices at once; this section marginalises them to find which group's choice actually moves the primary metrics (RQ2) --- and, new to this analysis, whether any preference it finds is corpus-specific or survives domain shift.

\textbf{In-domain.}
Table~\ref{tab:T5_group_marginal} and Figure~\ref{fig:hybrid_group_marginal} show the mean QWK when each group uses a TCN, a Transformer, or an LSTM, marginalising over all other groups.
Only \textbf{head pose} shows a clear preference --- TCN (0.545), ahead of LSTM (0.541) and Transformer (0.523), a spread of 0.022.
Macro-MAE agrees: head pose is again the only group with a meaningful spread, lowest (best) under the TCN (0.559) and worst under the Transformer (0.574).
The other four groups are essentially encoder-agnostic (QWK spread $\le0.006$).
\input{Table/T5_group_marginal}

\begin{figure}[ht]
\centering
\includegraphics[width=0.85\linewidth]{hybrid_group_marginal.png}
\caption{Per-group marginal effect of the encoder choice on in-domain QWK.}
\label{fig:hybrid_group_marginal}
\end{figure}

\textbf{Under shift.}
Table~\ref{tab:T10_group_marginal_unseen} and Figure~\ref{fig:hybrid_group_marginal_unseen} recompute the same marginal over the unseen-target cells.
Head pose again has the largest spread (0.010) and again prefers the TCN; the other groups' spreads (0.002--0.004) flip winners arbitrarily --- noise.
The consistency across regimes is what elevates the head-pose rule from a tuning accident to a property of the signal: head dynamics such as nodding and turning away are temporally local and convolution-friendly, while the gaze, landmark, and action-unit streams are insensitive to the encoder choice in both regimes.
\input{Table/T10_group_marginal_unseen}

\begin{figure}[ht]
\centering
\includegraphics[width=0.85\linewidth]{hybrid_group_marginal_unseen.png}
\caption{The same per-group marginal computed over the unseen-target cells: head pose
again shows the largest spread and again prefers the TCN, so the in-domain preference is
not corpus-specific.}
\label{fig:hybrid_group_marginal_unseen}
\end{figure}

\textbf{Conclusion.}
Of the five behavioural groups, only head pose has a clear encoder preference --- a TCN --- and that preference survives domain shift (RQ2).
The decomposition is a sound, interpretable framework whose tunable degrees of freedom are fewer than the 243-cell grid suggests, and most of its benefit must therefore come from \emph{decomposing and fusing} rather than from the per-group encoder search --- which is exactly what the next section measures.

\FloatBarrier
\section{Effect of I3D Fusion}
\label{sec:i3d-effect}

Chapter~4 predicted, from the error-overlap analysis of Section~\ref{sec:openface-vs-i3d}, that fusing the I3D stream should pay.
This section measures the prediction with the strongest design available in the grid: \textbf{pairing each OpenFace-only configuration with its exact +I3D twin} (same five encoders), so that the distribution of paired QWK differences \emph{is} the I3D effect, free of the configuration as a confounder.
Figure~\ref{fig:i3d_paired_delta} reports the paired differences in three evaluation regimes: the cells whose test corpus was seen in training, the two cross-corpus cells, and the private set.

\begin{figure}[ht]
\centering
\includegraphics[width=0.88\linewidth]{i3d_paired_delta.png}
\caption{Paired effect of I3D fusion: $\Delta$QWK between each configuration with and
without the I3D stream (same per-group encoders), by evaluation regime. Fusion is
decisively positive where the test corpus was seen in training, mildly negative
cross-corpus, and neutral with high variance on the private set.}
\label{fig:i3d_paired_delta}
\end{figure}

\textbf{Where the test corpus is seen, fusion pays decisively.}
Over the seen-target cells the mean paired gain is \textbf{+0.060 QWK and 94\% of the 972 pairs improve}.
In-domain on CMOSE this is the cash value of the complementarity measured in Section~\ref{sec:openface-vs-i3d}: the I3D-fused family is centred above the OpenFace-only family (median QWK 0.553 against 0.522) and above the baseline bar --- \textbf{82\% of the 243 fused configurations (199) clear 0.537}, against 26\% of the OpenFace-only ones --- with the like-for-like balanced metrics improving as well (fused medians: macro-accuracy 0.557, macro-MAE 0.543, against the best CE baseline's 0.535 and 0.547) and Cohen's kappa telling the same story (fused median 0.479 against the best base 0.436).
The gain is a property of the \emph{family}, not of one lucky configuration (RQ3).

\textbf{Beyond the training corpora, the gain evaporates.}
Cross-corpus the mean paired effect is \textbf{$-0.021$ QWK with only 26\% of pairs improving}; on the private set it is +0.005 with 52\% improving and very high variance ($\pm0.2$).
I3D's global appearance features drag corpus bias along --- the same mechanism behind the I3D MLP's fall from second to fourth place in the architecture ranking under shift (Section~\ref{sec:crossdomain-matrix}); the baseline result and the ablation result are one finding seen at two scales.

\textbf{Conclusion.}
Adding the I3D stream is decisively beneficial when the deployment domain is represented in training, and neutral-to-harmful beyond it (RQ3).
The practical rule: \emph{fuse I3D when the target domain is in the training distribution; rely on the OpenFace groups otherwise.}
Note the rule concerns the family average --- the single best private-set model in Section~\ref{sec:private-results} does carry I3D, but it is one tail of a wide, zero-centred distribution.

\FloatBarrier
\section{Comparison with Baselines}
\label{sec:error}

This section pins down the headline in-domain comparison on the primary metrics and then opens it up class by class to see where the gain actually lands.

\textbf{Best hybrid versus best baseline.}
Table~\ref{tab:T4_hybrid_topk} lists the top-10 configurations; the best overall is the I3D-fused hybrid \texttt{TCN\_T\_TCN\_LSTM\_T}, and every top configuration mixes at least two encoder types.
Figure~\ref{fig:hybrid_best_comparison} summarises the headline comparison on the three primary metrics: the best hybrid improves on the best baseline by \textbf{+0.068 QWK} (0.605 versus 0.537), lifts macro-accuracy (0.582 versus 0.535), and lowers macro-MAE (0.489 versus 0.547, lower is better).
All three primary metrics move in the same direction, so the gain is real --- but \textbf{modest}.
\input{Table/T4_hybrid_topk}

\begin{figure}[ht]
\centering
\includegraphics[width=0.95\linewidth]{hybrid_best_comparison.png}
\caption{Best hybrid versus best base model, in-domain on CMOSE.}
\label{fig:hybrid_best_comparison}
\end{figure}

\textbf{Where does the gain come from?}
The headline QWK gain is an average; the row-normalised confusion matrices (Figure~\ref{fig:confusion_best_models}) and the per-class F1 scores (Figure~\ref{fig:per_class_f1}) localise where the hybrid helps and where the problem remains open.

\begin{figure}[ht]
\centering
\includegraphics[width=0.9\linewidth]{confusion_best_models.png}
\caption{Confusion matrices of the best base versus the best hybrid (in-domain,
row-normalised).}
\label{fig:confusion_best_models}
\end{figure}

Relative to the best baseline, the hybrid improves recall on the ordinal extreme \textbf{Highly\nobreakdash-Engage} ($0.38\to0.51$) and on \textbf{Disengage} ($0.45\to0.55$), while keeping \emph{Engage} near 0.90 ($0.91\to0.90$).
However, the rarest class \textbf{Highly\nobreakdash-Disengage} stays unsolved and is slightly traded away (base recall $0.40\to$ hybrid 0.37): the model reallocates capacity toward the more frequent extreme.

\begin{figure}[ht]
\centering
\includegraphics[width=0.85\linewidth]{per_class_f1.png}
\caption{Per-class F1: best base versus best hybrid.}
\label{fig:per_class_f1}
\end{figure}

\textbf{Conclusion.}
The hybrid's in-domain improvement is real but \textbf{uneven}: it clears the baseline on all three primary metrics (QWK 0.605 versus 0.537) by strengthening the well-populated upper-engagement region and the middle disengaged class, but the $\sim$2--3\%-frequency \emph{Highly\nobreakdash-Disengage} class remains the principal open challenge (RQ1).
This is the in-domain picture; the next section shows it becomes more severe still once the model leaves its training corpus --- and, at the same time, that the architecture's advantage over the baseline \emph{widens} there.

\FloatBarrier
\section{Cross-Dataset Generalization}
\label{sec:private}

The decisive question is whether the hybrid's in-domain advantage survives, and pays off, on data the model was never trained on.
Chapter~4 established with the baselines that engagement models do not transfer across corpora, that accuracy masks the failure, that pooling source corpora is the most effective simple lever, and that in-domain rank barely predicts transfer.
This section first confirms the collapse on the hybrid, then establishes the claim that completes the thesis's contribution: \textbf{the hybrid generalizes better than the baselines} --- in every cell, and most of all on the self-collected private set.

\subsection{Cross-Dataset Matrix}
\label{sec:hybrid-crossdomain}

The hybrid cross-dataset heatmap (Figure~\ref{fig:crossdomain_hybrid}) shows the same pattern as the baseline matrix of Section~\ref{sec:crossdomain}: a healthy diagonal and an off-diagonal collapse of all three primary metrics --- the confirmation anticipated in Section~\ref{sec:ablation}.
The hybrid does not rescue cross-corpus transfer.

\begin{figure}[ht]
\centering
\includegraphics[width=0.88\linewidth]{crossdomain_hybrid.png}
\caption{Cross-domain generalisation, best hybrid per cell, on the three primary
metrics --- QWK, macro-accuracy, and macro-MAE (lower is better) --- plus raw accuracy,
which again stays high in the off-diagonal cells where the chance-corrected metrics
collapse.}
\label{fig:crossdomain_hybrid}
\end{figure}

\textbf{The accuracy deception persists.}
On DAiSEE$\rightarrow$CMOSE the best QWK any of the 486 hybrid runs achieves is 0.10, yet the accuracy panel of Figure~\ref{fig:crossdomain_hybrid} shows hybrids in that cell posting accuracies up to 0.69; on CMOSE$\rightarrow$DAiSEE the best QWK is 0.07 against accuracies near 0.50.
A second architecture family, developed independently of the baselines, thus lands in the same place: accuracy reports a partial success in exactly the cells where the chance-corrected metrics report chance-level agreement.
The metric frame of Section~\ref{sec:protocol-metrics} is therefore not an artefact of the monolithic models --- and this is the last time accuracy appears in the thesis's analysis.

\textbf{Within the collapse, the hybrid wins every cell.}
Comparing Figure~\ref{fig:crossdomain_hybrid} with Figure~\ref{fig:crossdomain_base} cell by cell, the best hybrid beats the best baseline in \textbf{all nine train$\times$test cells}: on the diagonals (0.605 vs 0.537 on CMOSE; 0.228 vs 0.166 on DAiSEE), in the collapsed cross-corpus cells (e.g.\ 0.096 vs 0.045 on DAiSEE$\rightarrow$CMOSE), and across the whole private column (0.309 vs 0.190 from CMOSE; 0.306 vs 0.256 from DAiSEE; 0.379 vs 0.285 from Combined).
The improvement is not a single tuned point but a uniform shift of the entire matrix --- the strongest form the family-level claim can take.

\subsection{The Private Set}
\label{sec:private-results}

The most consequential evaluation in this thesis is on the \textbf{private set} --- the 366 clips we collected and hand-labelled ourselves (Section~\ref{sec:private-collection}), processed through the identical OpenFace/I3D pipeline and used \textbf{for testing only}.
Because no model is ever trained on it, the private set is a genuine ``in-the-wild'' probe of deployment: it has its own recording conditions and its own label prior (58\% Engage, 31\% Highly-Engage, only 2.7\% Highly-Disengage).
For this set there is no in-domain option at all; the \emph{only} design lever is the choice of training source, which makes it the cleanest test of the two contributions of this thesis acting together --- the semantic-group architecture and the corpus-pooling insight of Section~\ref{sec:crossdomain}.
Figure~\ref{fig:private_by_source} and Table~\ref{tab:T6_private_by_source} report the best base model and the best semantic-group hybrid on the private set, for each training source.
\input{Table/T6_private_by_source}

\begin{figure}[ht]
\centering
\includegraphics[width=0.8\linewidth]{private_by_source.png}
\caption{Private-set generalisation by training source: best base versus best
hybrid, on QWK (higher is better) --- the headline generalisation metric. Macro-accuracy
and macro-MAE for the same models are in Table~\ref{tab:T6_private_by_source}.}
\label{fig:private_by_source}
\end{figure}

Two effects compound, and both favour this thesis's contributions.
\begin{enumerate}
  \item \textbf{Training source matters, and the combined corpus wins.}
  For both model families, training on \textbf{Combined} generalises best to the private set; a single source is markedly worse (best base QWK: Combined 0.285 versus DAiSEE 0.256 versus CMOSE 0.190).
  Pooling heterogeneous source corpora is the most effective simple route to an unseen target, exactly as the cross-dataset matrix of Chapter~4 predicted.
  \item \textbf{The hybrid generalizes better --- and helps more here than in-domain.}
  The semantic-group hybrid beats the best base model on QWK for \emph{every} training source, and the gap is \textbf{largest exactly where it matters}: under combined training the hybrid reaches \textbf{QWK 0.379 versus 0.285} for the best base, a \textbf{$+0.094$} improvement --- wider than the $+0.068$ in-domain CMOSE gain (Section~\ref{sec:error}).
  This is the crucial new claim of the section: the architecture's advantage is not an in-domain artefact that distribution shift erodes, it is a generalization property that distribution shift \emph{amplifies}.
  The overall best private-set model is the combined-trained I3D-fused hybrid \texttt{T\_T\_T\_LSTM\_T}, and it is best on both ordinal primaries: \textbf{QWK 0.379} and the lowest \textbf{macro-MAE 0.877} of any source (Table~\ref{tab:T6_private_by_source}), against the best base's 0.997.
  Macro-MAE corroborates the QWK ranking with one honest caveat: the CMOSE-only base model attains a deceptively low macro-MAE (0.75) despite the weakest QWK (0.19), because it predicts conservatively around the majority classes --- precisely the kind of false comfort that reporting all three primaries together is designed to catch.
\end{enumerate}

In other words, the architecture contribution and the combined-training insight are not independent curiosities: they \textbf{stack}, and they stack most strongly on the hardest, most realistic, self-collected target.

Figure~\ref{fig:private_confusion_combined} shows the row-normalised confusion of this headline model on the private set, the counterpart, under distribution shift, to the in-domain analysis of Section~\ref{sec:error}.
The ordinal structure is captured (mass concentrates on the diagonal and its neighbours): \emph{Engage} recall is 0.72 and \emph{Highly\nobreakdash-Engage} 0.53, with errors mostly to adjacent classes.
As in-domain, the rare disengaged classes are the failure mode under shift --- \emph{Disengage} recall is only 0.29 and \textbf{Highly\nobreakdash-Disengage collapses to 0.00} (all ten private HD clips are misread as DE/EG).

\begin{figure}[ht]
\centering
\includegraphics[width=0.6\linewidth]{private_confusion_combined.png}
\caption{Private-set confusion, combined-trained I3D hybrid
\texttt{T\_T\_T\_LSTM\_T} (QWK 0.379, macro-MAE 0.877), row-normalised.}
\label{fig:private_confusion_combined}
\end{figure}

\subsection{In-Domain versus Transfer}
\label{sec:hybrid-indomain-transfer}

Section~\ref{sec:indomain-vs-transfer} showed that a baseline's in-domain QWK barely predicts its transfer ($\rho=0.28$); the ablation population sharpens that warning.
Figure~\ref{fig:indomain_vs_generalization_hybrid} overlays the 972 trained hybrid models on the baseline scatter: within the hybrid family the Spearman correlation between in-domain QWK and unseen-target QWK is \textbf{negative} ($\rho=-0.31$) --- among closely related configurations, squeezing out more in-domain QWK mildly \emph{hurts} transfer, because the extra fit is corpus fit.
This puts the hybrid's generalisation advantage in its proper light: the architecture does not win by sitting further along an in-domain--transfer trend --- no such usable trend exists --- it wins by \textbf{lifting the whole trade-off}, sitting above the baseline cloud at every level of in-domain performance.
It also yields a practical warning for model selection: picking the single best in-domain configuration is not how the private-set winner is found; family-level robustness and pooled training are.

\begin{figure}[ht]
\centering
\includegraphics[width=0.85\linewidth]{indomain_vs_generalization_hybrid.png}
\caption{In-domain QWK versus mean unseen-target QWK with the hybrid ablation population
(grey) overlaid on the baseline configurations (coloured). Within the hybrid family the
correlation is negative ($\rho=-0.31$): more in-domain fit means more corpus fit. The
hybrid cloud sits above the baselines throughout --- the architecture lifts the
trade-off rather than riding it.}
\label{fig:indomain_vs_generalization_hybrid}
\end{figure}

\FloatBarrier
\section{Discussion}
\label{sec:gen-discussion}

\textbf{Why pooling is the effective lever.}
The cross-dataset matrix and the private-set result agree that training on the \emph{combined} corpus generalises best, and the dataset statistics of Section~\ref{sec:data} explain why.
CMOSE and DAiSEE are imbalanced in opposite directions --- CMOSE toward \emph{Engage}, DAiSEE additionally toward \emph{Highly\nobreakdash-Engage} --- so their union spans a wider range of label priors than either alone.
An unseen target such as the private set, with its own distinct prior, is consequently closer in distribution to the pooled corpus than to either single source, and the model sees both upper-engagement regimes in training rather than overfitting one.
Pooling is thus not a tuning trick but a direct consequence of prior coverage, which is why it helps with no change to the architecture or loss.

\textbf{Why the hybrid helps more under shift.}
That the architecture's margin over the baseline \emph{widens} off-domain ($+0.094$ versus $+0.068$) is consistent with the decomposition's design rationale.
Encoding each behavioural channel separately, and forcing each to be discriminative through its auxiliary head, yields a representation less tied to any single corpus's idiosyncrasies than a monolithic encoder that can latch onto a corpus-specific shortcut.
The negative in-domain--transfer correlation within the ablation ($\rho=-0.31$, Section~\ref{sec:hybrid-indomain-transfer}) is the same mechanism seen from the other side: whatever a configuration gains \emph{beyond} the family's level of in-domain fit is corpus fit, and it is the structured family --- not the most tuned member --- that carries the transfer advantage.

\textbf{Why the two feature families behave differently under shift.}
The paired I3D ablation (Section~\ref{sec:i3d-effect}) and the baseline architecture ranking (Section~\ref{sec:crossdomain-matrix}) tell one story at two scales: the OpenFace behaviour descriptors --- gaze angles, head-pose rotations, action-unit intensities --- are defined in subject-centred physical units that mean the same thing in any recording setup, whereas the I3D embedding encodes global appearance, which is exactly what changes between corpora.
Fusion therefore adds signal where the appearance statistics of the test data are represented in training, and adds bias where they are not.

\textbf{What the headline number does and does not mean.}
The best private-set result, QWK 0.379, should be read honestly.
It is a clear improvement over chance, over every single-source model, and over the best baseline ($+0.094$), and it shows the architecture and pooling contributions stacking on unseen data.
It is also, in absolute terms, \emph{moderate}: a QWK below 0.4, together with a macro-MAE of 0.877 --- almost a full ordinal step of average per-class error --- means the model captures the broad ordinal trend --- engaged versus disengaged --- but is unreliable at fine distinctions and, as the confusion matrix shows, blind to the rarest disengaged class.
For deployment this implies a system usable as an aggregate, classroom-level engagement signal, but one that should not be trusted to flag an individual highly-disengaged learner without human confirmation.

\textbf{The persistent failure mode.}
Across in-domain and shifted evaluation the same class fails: \emph{Highly\nobreakdash-Disengage}, the rarest (under 3\%) and the most decision-relevant.
In-domain it is traded away (Section~\ref{sec:error}); under shift it collapses to zero recall.
This is the clearest sign that the open problem is not architectural but one of data and objective: no encoder assignment can recover a class the training distribution barely contains, which is precisely why the imbalance-specific remedies and richer data proposed in Chapter~6 are the natural next step.

\FloatBarrier
\section{Chapter Summary}
\label{sec:hybrid-summary}

This chapter evaluated the semantic-group hybrid under the frame fixed in Chapter~4 --- after first re-validating that frame on the 486-configuration ablation population, where the metric block structure, the blindness of accuracy, and the cross-domain collapse all reproduced, establishing them as task properties (Section~\ref{sec:ablation}).
Only head pose has a clear encoder preference --- a TCN --- and the preference survives domain shift, so the design space is mostly flat and the benefit comes from decomposing and fusing rather than from the encoder search (Section~\ref{sec:group-marginal}, RQ2).
The paired I3D ablation localised the fusion effect by regime: decisively positive where the test corpus is seen in training (+0.060 QWK, 94\% of pairs; 82\% of fused configurations clear the baseline bar in-domain), neutral-to-harmful beyond it (RQ3) --- the family-level confirmation of Chapter~4's feature-complementarity analysis on the seen regime, and of its I3D rank-drop under shift (Section~\ref{sec:i3d-effect}).
In-domain the best hybrid beats the best baseline on all three primary metrics (+0.068 QWK, 0.605 versus 0.537), with the gain concentrated in the upper-engagement region and Highly\nobreakdash-Disengage the remaining open problem (Section~\ref{sec:error}, RQ1).
The generalization study (Section~\ref{sec:private}) established the new result that completes the thesis's argument: the hybrid beats the best baseline in \emph{every} cell of the 3$\times$3 matrix, compounding with combined-corpus training to the best private-set model (QWK 0.379, lowest macro-MAE 0.877, $+0.094$ over the baseline --- a wider margin than in-domain), while the in-domain--transfer correlation within the family is negative ($\rho=-0.31$): the architecture lifts the whole in-domain--transfer trade-off rather than riding it (RQ4).
In both regimes the rare Highly\nobreakdash-Disengage class remains the principal failure mode, collapsing entirely under distribution shift.
Chapter~6 draws the findings together, revisits the research questions, and outlines future work.

\end{document}
