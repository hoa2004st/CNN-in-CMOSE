# Progress Log

## Session 1 - 2026-Jun-04
- Complete: Rerun training and create raw prediction of test set of all models
- In progress: None
- Block: None
- Next session should: Make decision of choosing visualization methods. Getting ready for result analyse and thesis writing

## Session 2 - 2026-Jun-04
- Decision: thesis primary contribution = semantic-group hybrid model; spectral/frequency idea dropped (future work only)
- Complete: greenfield analysis/viz pipeline (src/analysis/aggregate.py, tables.py, make_thesis_artifacts.py; src/visualization/figbase.py + figures_*.py) -> 12 figures + 5 tables in outputs/thesis/
- Complete: documents/results_interpretation.md (per-figure takeaways), documents/references.bib (canonical entries + TODO placeholders for local PDFs)
- Complete: thesis scaffold documents/thesis/ (front matter + Ch1-5) following ThesisGuideline.txt
- Block: no PDF text tool in env -> 6 references.bib entries left as TODO to verify from documents/references/
- Revision (same session): elevated the self-collected PRIVATE set as a core contribution; added figures_private.py (private_by_source, indomain_cmose_vs_daisee, private_confusion_combined), tables T6/T7; reframed Ch1 contributions, abstract, Ch3 (private collection TODO), Ch4 (new 4.6 private section + DAiSEE-in-domain justification in 4.3), Ch5. Now 15 figures + 7 tables.
- Key revised finding: hybrid+combined best on private QWK 0.365 vs 0.285 base (+0.080, wider than in-domain); DAiSEE in-domain weak (QWK 0.139) justifies CMOSE-anchored ablation.
- Next session should: fill in TODO citations + Figure 3.1 diagram + private-set collection methodology paragraph; expand prose; tighten to page targets; (optional) regenerate artifacts with `python -m src.analysis.make_thesis_artifacts`

## Session 3 - 2026-Jun-08
- Goal: full rewrite of documents/thesis under the updated AGENTS.md rules (8-11 pages/chapter; losses/metrics/architectures/pipeline fully described; architecture drawn as a diagram; each result reaches a clear conclusion; strict title).
- Decisions taken with user: architecture diagrams drawn as **TikZ** (self-contained, compiles); cover/acknowledgment identity to be **provided by user** (placeholders <AUTHOR NAME> / <SUPERVISOR NAME> until then).
- Complete: rewrote all five body chapters + Abstract (added keywords) + Acknowledgment; removed the stale "auto-generated / do not edit by hand" headers and all draft TODO blockquotes; every Chapter-4 section now ends in an explicit Conclusion.
- Complete: Ch2 now fully defines the three temporal architectures, the three losses, and the six metrics with formulas; Ch3 gives exact dims, the leakage-free preprocessing, the full private-set collection methodology, exact loss/aux formulation, and the training + 3x3 eval protocols.
- Complete: added two TikZ figures in Ch3 (Fig. pipeline, Fig. hybrid architecture) + a semantic-group table; added `tikz` + libraries to main.tex preamble.
- Complete: enforced strict title "Student's engagement detection in online classes" in main.tex \TITLE and both covers (dropped old descriptive subtitle).
- Complete: glossary extended (RNN, BiLSTM, PCA, SVD, ReLU, GELU, AMP, CDF).
- Reconciled: README.md rewritten (Markdown chapter sources removed; .tex now hand-authored; make_thesis_artifacts still regenerates Figure/ + Table/ only; diagrams are TikZ). Renamed misspelled progess.md -> progress.md to match AGENTS.md.
- Note: thesis_latex.convert_chapters() safely skips the deleted .md, so `make_thesis_artifacts` regenerates Figure/Table only and will NOT overwrite the hand-written chapters.
- Numbers anchored to T1-T7 / class_distribution.csv: best baseline CMOSE QWK 0.537; best hybrid+I3D 0.590; private best = combined I3D hybrid T_TCN_LSTM_TCN_TCN QWK 0.403 vs 0.285 base.
- Block: cannot verify a PDF build in this env (no LaTeX toolchain confirmed); 6 references.bib entries still TODO.
- Next session should: fill cover/acknowledgment identity once provided; complete the 6 TODO citations from documents/references/; compile on Overleaf and fix any overfull boxes / TikZ sizing; consider a multi-seed rerun for confidence intervals.
- Follow-up (same session, figure polish per user request):
  - Legends moved OUTSIDE the plot area in every figure that had an in-axes legend (figures_loss, figures_hybrid, figures_private, figures_confusion) via `bbox_to_anchor=(1.02,1), loc="upper left"`; the multi-panel loss-curve figure now uses one figure-level legend. `savefig.bbox="tight"` keeps outside legends from being clipped.
  - Lowered figure resolution: `figbase.SAVE_DPI` (default **150**, was 300) controls `outputs/thesis/figures` DPI; override without editing code via env var `THESIS_FIG_DPI` (e.g. `$env:THESIS_FIG_DPI=200`). Lower SAVE_DPI also lowers the `documents/thesis/Figure` print copies (publish step only downsizes, never upscales).
  - Fixed `latexfmt.dataframe_to_latex` to re-emit the `\fittable{...}` wrapper (the generator had been dropping it, so regenerating tables would let wide ones overflow). Regeneration is now idempotent with the committed Table/*.tex.
  - Reran `python -m src.analysis.make_thesis_artifacts`; verified legends are outside, figures are ~half the pixels/size, and tables match the committed format.
- Follow-up (same session, diagrams): per user, **removed TikZ** (packages dropped from main.tex; both Ch3 tikzpictures replaced with compile-safe framed-box placeholders that keep captions + labels `fig:pipeline`/`fig:hybrid`). Added `documents/thesis/diagrams_mermaid.md` with the two diagrams as **Mermaid** so the user can view and **draw them by hand**, then export to `Figure/pipeline.png` + `Figure/hybrid_architecture.png` and replace the placeholder boxes with `\includegraphics`. Reconciled README + memory (no more TikZ).

## Session 4 - 2026-Jun-08 (I3D encoder: TCN-on-tiled -> MLP)
- Decision (user): the I3D feature is a single 1024-d clip vector (tiled to a pseudo-sequence by the loader), so feeding it through the TCN was degenerate. Replace the I3D path with an MLP, in BOTH the hybrid and the i3d_mlp baseline.
- FINAL architecture (after a few user iterations on dims):
  - Hybrid I3D stream (`OpenFaceTemporalI3DHybrid`): `_encode_i3d` mean-pools the tiled `(B,75,1024)` back to `(B,1024)`, then MLP `Linear 1024->256 -> ReLU -> Dropout0.3 -> Linear 256->128 -> ReLU -> LayerNorm(128)` => **128-d** embedding (groups stay 64-d). Concatenation = `5*64 + 128 = 448`; fusion `448->128->4`; I3D aux head `128->4`.
  - Baseline `i3d_mlp` = new `I3DVectorMLP`: mean-pool -> `1024->256->128->4` (matches the FlattenMLP head shape). build_model('i3d_mlp') returns it.
  - Only `src/models/models.py` changed; no plumbing changes (loader still tiles; model mean-pools). The mean-pool is a no-op vs the 1-D vector; a cleaner no-tiling refactor is possible later WITHOUT retraining (checkpoint-compatible).
- Mermaid diagrams rewritten to **two architecture diagrams** (baseline + hybrid) with **every layer + node count listed** (Diagram 1: openface_mlp/tcn/lstm/transformer + i3d baseline; Diagram 2: hybrid flow + the 3 group-encoder options).
- Thesis figure reconciliation: dropped the pipeline figure; Ch3 now has `fig:baseline` (in Baseline Models) + `fig:hybrid`; updated all `fig:pipeline` refs (Ch1 -> fig:hybrid, Ch3 summary -> fig:baseline) and the I3D-as-MLP wording (Ch3 multimodal variant + caption + features + i3d_mlp bullet; Ch5 limitation). README export names -> baseline_architecture.png / hybrid_architecture.png.
- Env: training uses `.venv-i3d` (torch 2.5.1+cu121, RTX 3060). Base python lacked tqdm (installed). RAM 32GB (~20GB free).
- IMPORTANT: assistant-launched **background jobs die when the user sends the next message** (saw it happen). The retrain is running in background `bv2p2qprp` (venv, final arch); if it dies, resume by re-running the same command chain in a terminal (idempotent; skips completed runs). 738 I3D runs (729 hybrid + 9 i3d_mlp); OpenFace-only hybrid (729) + 4 OpenFace base (36) preserved.
- PENDING (do when retrain finishes): the chain ends with evaluate_hybrid_full_matrix + evaluate_full_matrix + generate_hybrid_clip_predictions + make_thesis_artifacts (auto). Then **Step 5 numeric re-sync**: update Tables T2-T6 numbers, Ch4 §4.3/4.4/4.6 figures+numbers, Abstract + Ch5 headline numbers (best hybrid arch_key/QWK, private QWK, etc.) to the NEW results, and sanity-check the narrative.
- Run bv2p2qprp (overnight) completed **cmose + daisee** i3d_hybrid (243 each, valid) then **OOM'd on combined**: `load_i3d_dataset_matrices` tiled the 1024-d I3D vector to `(14109, 75, 1024)` = 4 GiB. FIX: load I3D at `target_frames=1` (no tiling; model mean-pools anyway) in `main.py` (`_load_i3d_splits_cached` call) and `src/evaluation/evaluate_full_matrix.py` (`_load_test_from_json`, `_load_private_test`, `_fit_i3d_norm`). This is checkpoint-compatible, so cmose/daisee (486) are NOT retrained. Resumed as bg **b6tsmuwr8** (skips 486, trains combined 243 + i3d_mlp 9, then eval+artifacts). I3D now loads as `(N,1,1024)`; normalization (3-D) and the model's mean-pool both handle it unchanged.
- Run b6tsmuwr8 then loaded combined fine (1-frame fix worked) and reached training start, but was **killed externally (exit 127, no traceback, GPU/RAM fine)** ~47 min in — likely the harness choking on ~31 MB of streaming tqdm output. Relaunched as **bjfljcsaq** with the whole chain redirected to `outputs/training_log/logs/i3d_rerun_v3.log` (harness captures ~nothing). cmose+daisee still skipped (486); combined now training with a warm OpenFace cache. If background runs keep dying, the reliable fallback is to run the same chain in a real terminal (idempotent/resumable).
- ROOT CAUSE of all the background-run failures: **C: drive was 100% full** (0 of 465 GB). The `outputs/.feature_cache` (OpenFace per-sample cache) had grown to **35.85 GB**; writes (checkpoints/logs) failed -> exit 127/120 with no traceback (NOT a code/harness bug; the OOM fix and combined training both work). Per user (option B), **deleted `outputs/.feature_cache`** (freed 36 GB); cache will rebuild (~26 GB) as the run proceeds. Relaunched as **blzvawvd0** (log -> outputs/training_log/logs/i3d_rerun_v4.log), 486 skipped, combined training. NOTE: disk margin is ~8 GB after cache refill — watch for disk-full if other processes consume C:. cmose+daisee (486) preserved and valid.
- blzvawvd0 (overnight) **completed ALL training**: combined hybrid 243/243 + i3d_mlp 9/9 (=> all 738 I3D runs done, +486 preserved). It then started eval and hit `OSError` because the cache had refilled the disk again (0 free). FINAL APPROACH for eval: cleared cache once more (35.7 GB freed) and ran the 4 eval/artifact steps with **cache DISABLED** (`OPENFACE_FEATURE_CACHE=""`) so nothing refills the disk -> bg **bfx81ef35** (log i3d_eval_v5.log). evaluate_hybrid_full_matrix `--model_types openface_temporal_i3d_hybrid` replaces only the stale i3d-hybrid rows (openface-only 2187 rows kept); evaluate_full_matrix rebuilds full_matrix (new i3d_mlp). Lesson for future reruns on this machine: keep the disk cache OFF or ensure >40 GB free.
- DONE. All training finished (738 I3D runs + 486 preserved). Eval chain (bfx81ef35) ran evaluate_hybrid_full_matrix + evaluate_full_matrix successfully; only generate_hybrid_clip_predictions failed (needs naive/predictions_by_clip.csv = clip-browser tool, NOT needed for the thesis). Ran `make_thesis_artifacts` directly -> 15 figures + 7 tables regenerated with new results.
- **Step 5 numeric re-sync COMPLETE** (Abstract, Ch1, Ch4, Ch5 updated to new MLP-I3D results):
  - Best in-domain hybrid: I3D `TCN_T_TCN_LSTM_T` QWK **0.605** acc 0.784 (was 0.590); I3D-fused family median **0.553**, **83%** clear the 0.537 baseline (was 0.546/69%). Best-vs-base **+0.068** (acc also up 0.784 vs 0.761).
  - i3d_mlp baseline improved: CE acc 0.773 / QWK 0.519 (was 0.764/0.493). DAiSEE in-domain best now i3d_mlp QWK **0.166** (T7).
  - Head pose still prefers TCN (0.545 vs LSTM 0.541 vs T 0.523, spread 0.022); other groups <=0.006.
  - Private headline: combined I3D hybrid `T_T_T_LSTM_T` QWK **0.379** acc 0.607 (was 0.403); base 0.285 -> **+0.094** (wider than in-domain +0.068). Single-source hybrids CMOSE 0.309 / DAiSEE 0.306. Private confusion recalls EG 0.72 / HE 0.53 / DE 0.29 / HD 0.00.
  - Error analysis: best hybrid recalls HE 0.38->0.51, DE 0.45->0.55, EG 0.91->0.90, HD 0.40->0.37.
  - All narratives still hold (I3D family above baseline, compounds on private). Verified: no stale numbers remain, all chapters balanced. Not committed (awaiting user).
